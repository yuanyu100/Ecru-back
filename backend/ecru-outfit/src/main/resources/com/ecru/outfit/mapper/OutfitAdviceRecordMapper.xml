<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecru.outfit.mapper.OutfitAdviceRecordMapper">

    <select id="selectByUserId" resultType="com.ecru.outfit.entity.OutfitAdviceRecord">
        SELECT * FROM outfit_advice_records
        WHERE user_id = #{userId}
          AND NOT (input_type = 3 AND occasion = 'HOME_DAILY')
        ORDER BY created_at DESC
    </select>

    <select id="selectByUserIdAndOccasion" resultType="com.ecru.outfit.entity.OutfitAdviceRecord">
        SELECT * FROM outfit_advice_records WHERE user_id = #{userId} AND occasion = #{occasion} ORDER BY created_at DESC LIMIT #{limit}
    </select>

    <select id="selectFavoritesByUserId" resultType="com.ecru.outfit.entity.OutfitAdviceRecord">
        SELECT * FROM outfit_advice_records
        WHERE user_id = #{userId}
          AND is_favorite = true
          AND NOT (input_type = 3 AND occasion = 'HOME_DAILY')
        ORDER BY created_at DESC
    </select>

    <select id="countByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM outfit_advice_records
        WHERE user_id = #{userId}
          AND NOT (input_type = 3 AND occasion = 'HOME_DAILY')
    </select>

    <update id="updateIsFavorite" parameterType="map">
        UPDATE outfit_advice_records SET
            is_favorite = #{isFavorite}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="selectHomeRecommendations" resultType="com.ecru.outfit.entity.OutfitAdviceRecord">
        <!--
            首页推荐记录的识别规则：
            1. input_type = 3，表示系统自动生成的首页搭配
            2. occasion = 'HOME_DAILY'，表示首页日常推荐场景
            3. is_deleted = 0，过滤掉逻辑失效记录
        -->
        SELECT *
        FROM outfit_advice_records
        WHERE user_id = #{userId}
          AND input_type = 3
          AND occasion = 'HOME_DAILY'
          AND is_deleted = 0
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <delete id="deleteHomeRecommendations" parameterType="java.lang.Long">
        <!--
            刷新首页推荐时，会先删掉旧的 HOME_DAILY 记录，
            再写入新生成的一批推荐，保证首页和详情读取到的是同一批数据。
        -->
        DELETE FROM outfit_advice_records
        WHERE user_id = #{userId}
          AND input_type = 3
          AND occasion = 'HOME_DAILY'
    </delete>



</mapper>
