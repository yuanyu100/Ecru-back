<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecru.user.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.ecru.user.entity.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultType="com.ecru.user.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="selectByPhone" resultType="com.ecru.user.entity.User">
        SELECT * FROM users WHERE phone = #{phone}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE phone = #{phone}
    </select>

    <select id="selectAdminUsers" resultType="com.ecru.user.entity.User">
        SELECT * FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAdminUsers" resultType="long">
        SELECT COUNT(*) FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%') OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
